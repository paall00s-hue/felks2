# 🐺 WolfLive Bot Controller

نظام متكامل لإدارة وتشغيل بوتات WolfLive عبر واجهة تيليجرام سهلة الاستخدام. يتيح المشروع تشغيل عدة حسابات في وقت واحد والتحكم فيها عن بُعد.

## 📋 المتطلبات الأساسية (Prerequisites)

لتشغيل هذا المشروع بنجاح، تحتاج إلى:

1. **نظام تشغيل Windows**: المشروع مصمم لبيئة ويندوز.
2. **.NET 9.0 SDK**: يجب تثبيت بيئة تشغيل .NET 9.0 أو أحدث. [تحميل من هنا](https://dotnet.microsoft.com/download/dotnet/9.0).
3. **توكن بوت تيليجرام**: احصل عليه من @BotFather.
4. **حسابات WolfLive**: بريد إلكتروني وكلمة مرور للحسابات التي تريد تشغيلها كبوتات.

---

## 🚀 طريقة التشغيل وإعداد التوكن

### 1. إضافة توكن البوت (Bot Token)
قبل التشغيل، يجب وضع توكن البوت الخاص بك في ملف نصي:
1. قم بإنشاء ملف جديد باسم `.bot_token` في المجلد الرئيسي للمشروع (بجانب `run.bat`).
2. افتح الملف والصق التوكن داخله فقط (بدون أي مسافات أو أسطر إضافية).
3. احفظ الملف.

### 2. تشغيل البوت
1. اضغط مرتين على ملف `run.bat`.
2. ستفتح نافذة سوداء (Terminal) تبدأ في تحميل المكتبات وتشغيل النظام.
3. عند ظهور رسالة `✅ بوت التليجرام يعمل: @BotName`، اذهب إلى التليجرام واضغط `/start`.

### 3. كيفية العمل
- **القائمة الرئيسية**: تتيح لك إضافة حسابات، عرض الحسابات النشطة، أو الدخول لأوضاع خاصة مثل الانضمام للمجموعات.
- **إضافة حساب**: سيطلب منك البوت البريد الإلكتروني وكلمة المرور، ثم نوع البوت الذي تريد تشغيله (مراقبة، سباق، وقت، إلخ).
- **تعدد الحسابات**: يمكنك تشغيل حتى 5 حسابات في وقت واحد، كل منها يعمل بشكل مستقل.

---

## 🗑️ بوت حذف الرسائل (Auto Delete Bot)

هذا البوت مخصص لمراقبة مجموعة معينة وحذف رسائل أعضاء محددين (مثل بوتات مزعجة أو أعضاء مقيدين) تلقائياً بعد مدة زمنية.

### كيف يعمل؟
1. يجب أن يكون الحساب المستخدم **مشرفاً (Admin)** في المجموعة ليمتلك صلاحية الحذف.
2. يراقب البوت الرسائل في المجموعة المحددة.
3. إذا تطابق معرّف المرسل (User ID) مع القائمة المحظورة، يتم وضع الرسالة في طابور الحذف.
4. يتم حذف الرسالة بعد انقضاء وقت التأخير المحدد.

### ملف الإعدادات (`auto_delete_config.json`)
يتحكم هذا الملف في إعدادات الحذف الافتراضية:

```json
{
  "DefaultDelay": 2,          // وقت الانتظار بالثواني قبل الحذف
  "TargetUserIds": "12345,67890"  // أرقام (IDs) الأعضاء المراد حذف رسائلهم، مفصولة بفواصل
}
```
*ملاحظة: يمكنك أيضاً تحديد هذه القيم يدوياً من خلال المحادثة مع البوت عند تفعيل وضع الحذف.*

---

## 🦅 بوت مراقبة المعززات (Booster Monitor)

يقوم هذا البوت بمراقبة رسائل بوتات الألعاب (مثل صيد، صياد، سباق) في القنوات أو المجموعات، ويقوم بالرد عليها تلقائياً بالأوامر المناسبة للمشاركة.

### ملف الإعدادات (`monitor_config.json`)
يحتوي على إعدادات الاستجابة والمجموعة المستهدفة:

```json
{
  "TargetGroupId": "0",       // رقم المجموعة التي سيقوم البوت بالمشاركة فيها (0 يعني سيطلب منك الإدخال)
  "DelaySeconds": 10,         // فترة الانتظار (Cooldown) بين العمليات لتجنب الحظر
  "Phrases": [                // قائمة البوتات التي يتم مراقبتها
    {
      "Name": "صياد",         // الاسم التعريفي
      "Command": "!صياد 3"    // الأمر الذي سيرسله البوت عند رصد رسالة
    }
    // ... يمكن إضافة المزيد
  ]
}
```
*ميزة هامة: عند تشغيل أي بوت يعتمد على مجموعة (مثل السباق أو الوقت)، سيتحقق من قيمة `TargetGroupId` في هذا الملف. إذا كانت `0`، سيطلب منك البوت إدخال رقم المجموعة ويقوم بحفظه تلقائياً لاستخدامه في المرات القادمة.*

---

## 🤖 أنواع البوتات المتوفرة

بالإضافة للحذف والمراقبة، يدعم النظام الأنواع التالية:

1. **🦅 بوت المراقبة (Monitor Bot)**:
   - الاسم في القائمة: "مراقبة المعززات".
   - الوظيفة: انتظار تنبيهات المعززات في الخاص، ثم الانضمام للمجموعة والصيد تلقائياً.

2. **🐎 بوت السباق (Race Bot)**:
   - الاسم في القائمة: "بووت الخيل".
   - الوظيفة: المشاركة التلقائية في سباقات الخيول، مع دعم التدريب التلقائي، التنبيه عند امتلاء الطاقة، وإعادة المشاركة.

2. **🧮 بوت أحسب (Calculator)**:
   - الوظيفة: حل مسائل الرياضيات بسرعة.

3. **📝 بوت كتابة (Writer)**:
   - الوظيفة: كتابة الجمل المطلوبة في أسرع وقت.

4. **🔄 بوت عكس (Reverse)**:
   - الوظيفة: عكس الكلمات المطلوبة.

5. **⏱️ بوت وقت (Time)**:
   - الوظيفة: الرد بأمر الوقت.

---

## 🛠️ ملاحظات للمطورين
- المشروع مبني باستخدام C# و .NET 9.0.
- يتم استخدام `Task.Run` للعمليات الخلفية لضمان عدم توقف واجهة التليجرام.
- يتم عزل كل بوت في `Task` منفصلة لضمان استقرار النظام عند تشغيل عدة حسابات.
- **Web API & Swagger**: يعمل النظام أيضاً كسيرفر ويب محلي (Web API).
  - عند التشغيل، يمكنك الوصول إلى وثائق الـ API عبر: `http://localhost:5000/swagger`
  - يمكن استخدام الـ API للتحكم في البوتات برمجياً أو دمج النظام مع تطبيقات أخرى.

---
**وقت والتاريخ:** 2026-01-03
